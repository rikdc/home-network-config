all:
  children:
    dns:
      hosts:
        192.168.88.2:
          ansible_user: root
        192.168.88.3:
          ansible_user: root
    proxmox:
      hosts:
        proxmox-server:
    mysqldev:
      hosts:
        mysql-dev-server:
    postgresdev:
      hosts:
        postgres-dev-server:
    storage:
      hosts:
        rafiki.local:
        zazu.local:
