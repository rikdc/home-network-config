all:
  children:
    dns:
      hosts:
        192.168.88.2:
          ansible_user: root
        192.168.88.3:
          ansible_user: root
    mysql:
      hosts: simba.local
    postgres:
      hosts: nala.local
    storage:
      hosts:
        rafiki.local:
        zazu.local:
