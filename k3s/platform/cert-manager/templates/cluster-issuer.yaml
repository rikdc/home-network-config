{{- if and .Values.certmanager.enabled .Values.externalSecrets }}
# Wait for certmanager webhook to be ready
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ca-key-pair
  namespace: cert-manager
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "1"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: ca-key-pair
    template:
      type: kubernetes.io/tls
  data:
    - secretKey: tls.crt
      remoteRef:
        key: pki_homelab/intermediate/cert
    - secretKey: tls.key
      remoteRef:
        key: pki_homelab/intermediate/private_key
{{- end }}
---
{{- if and .Values.certmanager.enabled .Values.externalSecrets }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-issuer
  namespace: cert-manager
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "2"
spec:
  ca:
    secretName: ca-key-pair
{{- end }}
